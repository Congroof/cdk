# 实施计划

1. 加载后端路由、CDK、反馈与质量规范。
2. 删除 6 条旧路由及只服务这些路由的处理函数。
3. 保留租户共享逻辑，移除旧通用接口兼容性测试。
4. 更新 README、API 文档、CDK/反馈规范及接口清单。
5. 搜索已删路径，区分历史任务记录与现行实现/文档。
6. 运行后端 fmt/test/clippy/build 和前端 build。
7. 审查 diff，确认 SkinForge 8 个入口与管理后台 25 个入口均未删除。

## 风险点

- `/api/client/activate` 是未认证的 admin 租户绑定入口，删除时不能误删租户 `user_activate` 或共享 `activate_for_owner`。
- 删除匿名反馈写入口不能顺手收紧历史数据查询，否则会改变后台可见性。
- API 文档中的租户接口目前与旧通用接口共用章节，需重写标题和示例而不是整段删除。
